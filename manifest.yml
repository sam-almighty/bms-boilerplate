applications:
- disk_quota: 512M
  host: ${cf.app.name}
  name: ${cf.app.name}
  instances: 1
  memory: 1GB
  timeout: 180
  domain: ${cf.domain}
  buildpack: ${cf.buildpack.node}
  stack: ${cf.stack}
env:
  REGION: ${cf.region}
