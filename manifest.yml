applications:
- disk_quota: ${cf_disk_quota}
  name: ${cf_appname}
  host: ${cf_hostname}
  instance4s: ${cf_instances}
  memory: ${cf_memory}
  timeout: ${cf_timeout}
  domain: ${cf_domain}
  buildpack: ${cf_buildpack_node}
  stack: ${cf_stack}
env:
  REGION: ${cf_region}
