applications:
- disk_quota: 512M
  host: bms-boilerplate_${version.name}_BLUE
  name: bms-boilerplate_${version.name}_BLUE
  instances: 1
  memory: 1GB
  timeout: 180
  domain: ${cf.domain}
  buildpack: ${cf.buildpack.node}
  stack: ${cf.stack}
env:
  REGION: ${cf.region}
