applications:
- disk_quota: 512M
  host: ${app.name}
  name: ${app.name}
  instances: 1
  memory: 1GB
  timeout: 180
  domain: ${cf.domain}
  buildpack: ${cf.buildpack.node}
  stack: ${cf.stack}
env:
  REGION: ${cf.region}
