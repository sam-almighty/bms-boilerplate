applications:
- disk_quota: 512M
  host: imftemplatecatalog
  name: imftemplatecatalog
  instances: 1
  memory: 1GB
  timeout: 180
  domain: ${cf_domain}
  buildpack: ${cf_buildpack_node}
  stack: ${cf_stack}
env:
  REGION: ${cf_region}
